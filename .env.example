# Your server's local IP address (used in the TLS certificate)
NAS_IP=192.168.0.100

# Where to store certs and uploads on the host
CERT_PATH=./certs
UPLOAD_PATH=./uploads

# Unraid users: use your appdata/data shares instead, e.g.:
# CERT_PATH=/mnt/user/appdata/fujidrop
# UPLOAD_PATH=/mnt/user/data/camera-uploads

# Dashboard port (plain HTTP, no certificate warning)
# DASHBOARD_PORT=3000

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=info
