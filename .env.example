# Your NAS/server's local IP address (used in the TLS certificate)
NAS_IP=192.168.0.100

# Where uploaded photos/videos are stored inside the container
UPLOAD_DIR=/uploads

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=info
